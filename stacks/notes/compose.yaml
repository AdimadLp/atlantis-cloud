# Notes Services - Affine
# Separate docker-compose file for note-taking services
services:
  #--------------------------------------------------------------------------
  # Affine Main Service
  #--------------------------------------------------------------------------
  affine:
    image: ghcr.io/toeverything/affine-graphql:stable
    container_name: affine
    ports:
      - "3010:3010"
    depends_on:
      redis:
        condition: service_healthy
      postgres:
        condition: service_healthy
      affine_migration:
        condition: service_completed_successfully
    volumes:
      # custom configurations
      - /opt/notes/storage:/root/.affine/storage
      - /opt/notes/config:/root/.affine/config
    secrets:
      - db_password
    command: >
      sh -c '
      DB_PASSWORD=$$(cat /run/secrets/db_password)
      export DATABASE_URL="postgresql://affine_user:$$DB_PASSWORD@postgres:5432/affine"
      exec node ./dist/index.js
      '
    environment:
      - REDIS_SERVER_HOST=redis
      - AFFINE_INDEXER_ENABLED=false
    networks:
      - notes_network
    restart: unless-stopped
    healthcheck:
      test:
        [
          "CMD-SHELL",
          'node -e "require(''http'').get(''http://localhost:3010'', (res) => process.exit(res.statusCode === 200 ? 0 : 1)).on(''error'', () => process.exit(1))"',
        ]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s

  #--------------------------------------------------------------------------
  # Affine Migration Service
  #--------------------------------------------------------------------------
  affine_migration:
    image: ghcr.io/toeverything/affine-graphql:stable
    container_name: affine_migration
    volumes:
      # custom configurations
      - /opt/notes/storage:/root/.affine/storage
      - /opt/notes/config:/root/.affine/config
    secrets:
      - db_password
    command: >
      sh -c '
      DB_PASSWORD=$$(cat /run/secrets/db_password)
      export DATABASE_URL="postgresql://affine_user:$$DB_PASSWORD@postgres:5432/affine"
      exec node ./scripts/self-host-predeploy.js
      '
    environment:
      - REDIS_SERVER_HOST=redis
      - AFFINE_INDEXER_ENABLED=false
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    networks:
      - notes_network
    restart: "no"

  #--------------------------------------------------------------------------
  # Redis Cache Service
  #--------------------------------------------------------------------------
  redis:
    image: redis
    container_name: affine_redis
    volumes:
      - notes_redis_data:/data
    networks:
      - notes_network
    healthcheck:
      test: ["CMD", "redis-cli", "--raw", "incr", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped

  #--------------------------------------------------------------------------
  # PostgreSQL Database Service
  #--------------------------------------------------------------------------
  postgres:
    image: pgvector/pgvector:pg16
    container_name: affine_postgres
    volumes:
      - notes_postgres_data:/var/lib/postgresql/data
    environment:
      POSTGRES_USER_FILE: /run/secrets/db_username
      POSTGRES_PASSWORD_FILE: /run/secrets/db_password
      POSTGRES_DB: affine
      POSTGRES_INITDB_ARGS: "--data-checksums"
    secrets:
      - db_username
      - db_password
    networks:
      - notes_network
    healthcheck:
      test: ["CMD", "pg_isready", "-U", "affine_user", "-d", "affine"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped

#--------------------------------------------------------------------------
# Volumes - for persistent data
#--------------------------------------------------------------------------
volumes:
  notes_redis_data:
    name: notes_redis_data
  notes_postgres_data:
    name: notes_postgres_data

#--------------------------------------------------------------------------
# Secrets - for sensitive data
#--------------------------------------------------------------------------
secrets:
  db_username:
    file: /opt/secrets/notes/db_username
  db_password:
    file: /opt/secrets/notes/db_password

#--------------------------------------------------------------------------
# Networks - for service isolation
#--------------------------------------------------------------------------
networks:
  notes_network:
    driver: bridge
    name: notes_network
